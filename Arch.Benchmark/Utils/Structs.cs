namespace Arch.Test; 

public struct Transform {
    public float x, y;
}
    
public struct Velocity {
    public float x, y;
}

public struct Rotation { public float x, y, z, w; }

public struct AI{}